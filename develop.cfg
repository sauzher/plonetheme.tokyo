[buildout]
extends = buildout.cfg

deprecation-warnings = on
show-picked-versions = true

always-checkout = force
auto-checkout = *
sources = sources


[instance]
eggs +=
    pdbpp
    plone.reload
    Products.PDBDebugMode


[sources]
collective.sidebar = git git://github.com/collective/collective.sidebar.git branch=master
plone.app.theming = git git://github.com/plone/plone.app.theming.git branch=issue_176_bundle_without_rules
plone.app.z3cform = git git://github.com/plone/plone.app.z3cform.git branch=bootstrap4